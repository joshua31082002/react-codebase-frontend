services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4173:4173"
    command: ["sh", "-c", "sed -i 's/listen       80;/listen       4173;/' /etc/nginx/conf.d/default.conf && nginx -g 'daemon off;'"]
