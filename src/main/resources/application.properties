server.address=0.0.0.0
server.port=${PORT:8080}
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/healthdb}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:healthapp}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:healthapp}
spring.datasource.hikari.maximum-pool-size=${SPRING_DATASOURCE_MAX_POOL_SIZE:5}
